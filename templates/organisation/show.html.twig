{% extends 'base.html.twig' %}

{% block title %}Organisation{% endblock %}

{% block body %}
    <h1>Organisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ organisation.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ organisation.designation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ organisation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organisation_index') }}">retour à la liste </a>

    <a href="{{ path('app_organisation_edit', {'id': organisation.id}) }}"><span class="fa fa-edit"></span></a>

    {{ include('organisation/_delete_form.html.twig') }}
{% endblock %}
