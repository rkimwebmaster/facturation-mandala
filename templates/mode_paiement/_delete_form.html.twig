<form method="post" action="{{ path('app_mode_paiement_delete', {'id': mode_paiement.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_paiement.id) }}">
    <button class="btn btn-danger"><span class="fa fa-trash"> </span>Supprimer </button>
</form>
